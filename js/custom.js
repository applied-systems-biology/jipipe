$(document).ready(function(){
  $('.landing-features').slick({
    infinite: true,
    dots: true,
    slidesToShow: 1,
    autoplay: true,
    autoplaySpeed: 10000,
    centerMode: true,
    centerPadding: '5rem'
  });
});
