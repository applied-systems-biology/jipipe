image: maven:3-eclipse-temurin-21-jammy

stages: [package, publish]

variables:
  GIT_STRATEGY: fetch
  GIT_DEPTH: "1"

distribution_package:
  stage: package
  interruptible: true
  retry: 1
  before_script:
    - apt-get update
    - apt-get install -y git git-lfs zip unzip
    - git lfs install
    - git config --global --add safe.directory "$CI_PROJECT_DIR"
  script:
    - git clone --depth 1 https://asb-git.hki-jena.de/RGerst/jipipe-distribution-files.git
    - cd jipipe-distribution-files/scripts
    - ./step1-publish-snapshot-ci.sh "" "${CI_PIPELINE_IID}"
    - ./step2-create-zip.sh
    - ./step3-build-packages.sh
    - cd ..
    - OUTDIR="$(ls -d *-snapshot${CI_PIPELINE_IID} | head -n 1)"
    - test -n "$OUTDIR" || { echo "Snapshot output dir not found"; ls -lah; exit 1; }
    - ls -lah "$OUTDIR"
  artifacts:
    when: always
    expire_in: 1 week
    paths:
      - jipipe-distribution-files/*-snapshot${CI_PIPELINE_IID}/*

upload_to_package_registry:
  stage: publish
  needs:
    - job: distribution_package
      artifacts: true
  image: alpine:3.20
  before_script:
    - apk add --no-cache curl jq
    # Locate output and compute version string from step1 markers
    - OUTDIR="$(ls -d jipipe-distribution-files/*-snapshot${CI_PIPELINE_IID} | head -n 1)"
    - VERSION="$(cat jipipe-distribution-files/scripts/initial-scijava/jipipe_version)-snapshot${CI_PIPELINE_IID}"
    - PKG="jipipe"
    - echo "Publishing $OUTDIR to Generic Packages as $PKG/$VERSION"
    # Build a small manifest we can publish as an artifact for convenience
    - echo "# JIPipe $VERSION download URLs" > urls-${VERSION}.txt
  script:
    # Upload every file in the snapshot folder
    - |
      set -euo pipefail
      BASE_API="$CI_API_V4_URL/projects/$CI_PROJECT_ID/packages/generic/$PKG/$VERSION"
      for f in "$OUTDIR"/JIPipe-*; do
        name="$(basename "$f")"
        echo "Uploading $name …"
        # PUT/Upload using CI_JOB_TOKEN to the Generic Packages endpoint
        curl --retry 3 --fail -sS \
          --header "JOB-TOKEN: $CI_JOB_TOKEN" \
          --upload-file "$f" \
          "$BASE_API/$name"
        echo "$BASE_API/$name" >> "urls-${VERSION}.txt"
      done
    - echo "Done. URLs written to urls-${VERSION}.txt"
  artifacts:
    when: always
    expire_in: 3 months
    paths:
      - urls-*.txt
